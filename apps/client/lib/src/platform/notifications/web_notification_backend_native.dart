import 'package:broker_client_flutter/broker_client_flutter.dart';

/// Native platforms use the plugin backend, so there is no override.
FlutterLocalNotificationBackend? createWebNotificationBackend() => null;
