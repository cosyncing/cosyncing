/// Connects the handoff participant registry to the browser page.
///
/// Native platforms have no browser coordinator and update through the signed
/// broker/runtime flow, so this is a no-op.
void installWebHandoffBridge() {}
