# Flutter Source

Flutter app source will live here after `flutter create .` is run from the repo root.

Expected first modules:

```text
app/
features/broker_profiles/
features/connection/
protocol/
platform/
shared/widgets/
```
