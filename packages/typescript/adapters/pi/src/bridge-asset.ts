/**
 * Compatibility surface: the embedded bridge asset (extension bytes, hashes, legacy fixture) is
 * owned by the shared pi-engine package; this module preserves the historical adapter import path.
 */
export * from '@cosyncing/pi-engine/bridge-asset';
