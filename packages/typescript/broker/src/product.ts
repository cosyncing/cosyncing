/** Broker-local import surface for the core-owned public product identity. */
import { PRODUCT_IDENTITY } from '@cosyncing/protocol/product';

export { PRODUCT_IDENTITY };
export type ProductIdentity = typeof PRODUCT_IDENTITY;
