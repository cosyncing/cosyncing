name: broker_crypto
description: >
  Pure-Dart cryptographic primitives for cosyncing transport pairing.
version: 0.1.0

publish_to: 'none'

environment:
  sdk: ^3.12.2

dependencies:
  cryptography: ^2.7.0
  meta: ^1.15.0

dev_dependencies:
  test: ^1.25.0
  very_good_analysis: ^9.0.0
